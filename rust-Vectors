// Online Rust compiler to run Rust program online

fn main() {
    let mut v = vec![1, 2, 3];
    let mut vec: Vec<u32> = (0..10).collect();
  
    let three = v[2];
    v[1] = v[1] + 5;
   // println!("{}  = three {}",v[1],three);
}
